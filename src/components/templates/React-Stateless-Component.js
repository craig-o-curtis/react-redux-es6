import React from 'react';
import PropTypes from 'prop-types';

const $NAME = (props) => {
  return (
    <div></div>
  );
};

$NAME.PropTypes = {
  myProp : PropTypes.string.isRequired
};

export default $NAME;